# ═══════════════════════════════════════════════════════════════
# SECRETS — NEVER commit this file (in .gitignore)
# Only fill in the providers you actually use.
# ═══════════════════════════════════════════════════════════════

# OpenAI (if llm.*.model starts with "openai/")
OPENAI_API_KEY=sk-...

# Anthropic (if llm.*.model starts with "anthropic/")
ANTHROPIC_API_KEY=sk-ant-...

# OpenRouter (if llm.*.model starts with "openrouter/")
OPENROUTER_API_KEY=sk-or-...
